version = getSubprojectVersion(project)

moduleDependencies(project, [
	'fabric-api-base',
	'fabric-resource-loader-v1'
])

loom {
	accessWidenerPath = file('src/main/resources/fabric-creative-mode-tab-api-v1.accesswidener')
}
